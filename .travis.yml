language: java
sudo: true
before_install:
  - echo "MAVEN_OPTS='-Xms512m -Xmx1024m -XX:PermSize=256m -XX:MaxPermSize=512m'" > ~/.mavenrc
script: travis_wait 40 mvn clean install --quiet
jdk:
    - oraclejdk7
